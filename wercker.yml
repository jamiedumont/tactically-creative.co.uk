box: wercker/default
build:
  steps:
    - arjen/hugo-build:
        version: 0.13
        theme: tacticallycreative
deploy:
    steps:
        - s3sync:
            key-id: $KEY
            key-secret: $SECRET
            bucket-url: s3://tactically-creative.co.uk
            source_dir: public/