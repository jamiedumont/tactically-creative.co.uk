box: wercker/default
build:
  steps:
  - arjen/hugo-build:
    version: 0.13
deploy:
  steps:
    - s3sync:
      key-id: $KEY
      key-secret: $SECRET
      bucket-url: $BUCKET
      source_dir: public/